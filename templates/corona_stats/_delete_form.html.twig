<form method="post" action="{{ path('corona_stats_delete', {'id': corona_stat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ corona_stat.id) }}">
    <button class="btn">Delete</button>
</form>
